services:
  data:
    image: 'postgres:15-alpine'
    container_name: 'data'
    restart: 'always'
    volumes:
      - 'data:/var/lib/'
    environment:
      - 'POSTGRES_DB=escuela'
      - 'POSTGRES_PASSWORD=escuela'
      - 'POSTGRES_USER=12345678'
    ports:
      - '5432'

volumes:
  'data':